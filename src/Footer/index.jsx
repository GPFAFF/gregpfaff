import React from 'react'
import './index.scss';

const Footer = ({ className }) => {
  return (
    <footer className={className}>
      Here is the footer
    </footer>
  )
};

export default Footer;
