import React from 'react';
import './index.scss';

const Footer = ({ className, ref }) => (
  <footer
    className={className}
    ref={ref}
  >
    Here is the footer
  </footer>
);

export default Footer;
