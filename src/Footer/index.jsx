import React from 'react'
import './index.scss';

const Footer = ({ className, ref }) => {
  return (
    <footer
      className={className}
      ref={ref}
    >
      Here is the footer
    </footer>
  )
};

export default Footer;
